enum ItemType {
  unknown,
  image,
  style,
  script,
  navigation,
  vector,
  font,
  video,
  audio,
  document,
  cover,
  smil,
}

enum Namespace {
  dc,
  opf,
}

enum EpubDirection {
  ltr,
  rtl,
  lro,
  rlo,
}

enum EpubProperties {
  scripted,
}
